import React, { useEffect } from 'react';

import storage from 'electron-json-storage'

import './styles.css'

export default () => {

  useEffect(() => {

    // storage.remove('user')

  }, [])

  return (
    <div>
      home
    </div>
  );

};